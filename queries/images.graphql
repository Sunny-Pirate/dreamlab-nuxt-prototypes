query getImageById($id: ID!) {
    uploadFile(id: $id){
        data {
            attributes {
                name
                size
                hash
                url
                provider
                mime
                alternativeText
                caption
            }
        }
    }
}
